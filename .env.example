# ============================================================
# AZURE AD CONFIGURATION (REQUIRED FOR NVIDIA USERS)
# ============================================================
# If you're using NVIDIA's Azure AD, you MUST provide your own Client ID
# Request IT to create an Azure AD app registration with these settings:
#   - Name: Top 5 Things Generator
#   - Supported account types: Single tenant
#   - API Permissions (Delegated): User.Read, Calendars.Read, Mail.Read
#   - No redirect URI needed (device code flow)
#
# Once IT provides the Client ID, uncomment and set it below:
# CLIENT_ID=your-client-id-from-IT

# ============================================================
# OPTIONAL CONFIGURATION
# ============================================================

# Number of days to analyze (default: 30)
# DAYS_BACK=30

# Token cache location (default: ./data/token_cache.json)
# TOKEN_CACHE_FILE=./data/token_cache.json

