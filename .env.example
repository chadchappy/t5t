# Optional: Number of days to analyze (default: 30)
# DAYS_BACK=30

# Optional: Token cache location (default: ./data/token_cache.json)
# TOKEN_CACHE_FILE=./data/token_cache.json

# Note: No Azure AD app registration required!
# The app uses Microsoft's public client ID for device code flow authentication.
# You'll authenticate once when you run the container.

